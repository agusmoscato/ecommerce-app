import { View, Text } from 'react-native';

import { styles } from './styles';

const Payment = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.TextTitle}>Metodos de pago</Text>
    </View>
  );
};

export default Payment;